<h1>Studios!</h1>
<% require "pry"; binding.pry %>
<% @studios.each do |studio| %>
  <%= studio.name %>
  <section id=movie>
  <% studio.movies.each do |movie| %>
      <%= movie.title %>
  <% end %><br>
  </section>
<% end %>
